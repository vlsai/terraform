<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sailv.terraform.analysis.infrastructure.database.mapper.TemplateQuotaResourceMapper">

    <insert id="insertBatch">
        INSERT INTO t_mp_template_resource (
            id,
            template_id,
            resource_type,
            quota_type,
            quota_requirement,
            create_time,
            update_time
        ) VALUES
        <foreach collection="collection" item="resource" separator=",">
            (
                #{resource.id},
                #{resource.templateId},
                #{resource.resourceType},
                #{resource.quotaType},
                #{resource.quotaRequirement},
                #{resource.createTime},
                #{resource.updateTime}
            )
        </foreach>
    </insert>
</mapper>
